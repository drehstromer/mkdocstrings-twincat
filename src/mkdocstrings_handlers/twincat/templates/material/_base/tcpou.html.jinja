{% block title %}{{ data.name }} - TwinCAT POU Documentation{% endblock %}

{% block content %}
<div class="doc-section">
    <h1 class="doc-section-title">{{ data.name }}</h1>
    
    <div class="doc-item">
        <span class="badge badge-primary">POU</span>
        {% if data.extends %}
        <span class="doc-item-name">Extends:</span>
        <span class="doc-item-type">{{ data.extends }}</span>
        {% endif %}
        
        {% if data.implements %}
        <div class="doc-item">
            <span class="doc-item-name">Implements:</span>
            <ul>
                {% for interface in data.implements %}
                <li>{{ interface }}</li>
                {% endfor %}
            </ul>
        </div>
        {% endif %}
    </div>
    
    {% include "tcdocumentation.html.jinja" %}
    
    {% if data.declaration %}
    <div class="doc-section">
        <h3 class="doc-section-title">Declaration</h3>
        <pre><code>{{ data.declaration }}</code></pre>
    </div>
    {% endif %}
    
    {% include "tcvariable_section.html.jinja" %}
    
    {% if data.implementation %}
    <div class="doc-section">
        <h3 class="doc-section-title">Implementation</h3>
        <pre><code>{{ data.implementation }}</code></pre>
    </div>
    {% endif %}
    
    {% if data.methods %}
    <div class="doc-section">
        <h3 class="doc-section-title">Methods</h3>
        
        {% for method in data.methods %}
            {% include "tcmethod.html.jinja" %}
        {% endfor %}
    </div>
    {% endif %}
    
    {% if data.properties %}
    <div class="doc-section">
        <h3 class="doc-section-title">Properties</h3>
        
        {% for property in data.properties %}
            {% include "tcproperty.html.jinja" %}
        {% endfor %}
    </div>
    {% endif %}
</div>
{% endblock %}
